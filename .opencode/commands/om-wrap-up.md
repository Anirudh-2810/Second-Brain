---
description: "End-of-session review. Verify new notes are filed and linked, indexes updated, learnings persisted. Auto-run on 'wrap up'."
---

Run the session wrap-up.

1. **Verify every note created this session**: has YAML frontmatter (`date`, `description`, `tags`), links to at least one existing note, and lives in the right folder per AGENTS.md routing
2. **Wiki changes**: if any `wiki/01-Areas/` (or 00/02) pages were created or edited, confirm `wiki/index.md` lists them and `wiki/log.md` has an entry
3. **Persist learnings**: route anything still only in conversation into `brain/` (decisions → Key Decisions, friction → Gotchas, recurring themes → Patterns) using the routing table
4. **Wins**: log achievements in `brain/Wins.md` with links to evidence notes
5. **North Star**: offer to update `brain/North Star.md` if goals or focus shifted
6. **Daily note**: append a brief session summary to today's `daily/` note if not already there
7. If many notes were created, suggest `/om-vault-audit`

Report: files touched, brain notes updated, open threads left behind. Zero data loss — never delete; move only with confirmation.
