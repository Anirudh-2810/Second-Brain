---
description: "Correction sweep. Apply a corrected fact at its single source, then fix every restatement of it across the vault."
---

Apply this correction everywhere it appears: $ARGUMENTS

Protocol:
1. **Find the single source**: locate the one note where this fact authoritatively lives (per Write-Correctness Law 1). Correct it there first.
2. **Grep sweep**: search the whole vault for direct restatements of the old fact (exact wording, names, numbers).
3. **Paraphrase hunt**: grep alone misses reworded claims — reread candidate pages that discuss the same topic and check for the same claim in different words.
4. **Fix restatements in the same pass** so no stale copies survive.
5. **Preserve history**: dated records of what was believed at the time stay as written — correct forward-looking statements, never rewrite historical logs.
6. Report: source corrected, every restatement found and fixed, anything left untouched and why.
